#!/usr/bin/env bash
set -euo pipefail

repo_root="$(git rev-parse --show-toplevel)"
cd "$repo_root"

echo "[post-commit] publishing..."
if ! ./scripts/publish.sh; then
  echo "[post-commit] publish failed (commit already created)."
  exit 0
fi
